{
  "name": "zaloridebot",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "devDependencies": {
    "@types/node": "^22.13.10"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
